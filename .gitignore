# Secrets
.env
.env.*
!.env.example

# Phase-1 published mapping is generated per-run; keep it out unless you want history
# docs/tasks/linear-publish.yaml   # (uncomment to ignore)

# OS / tooling
.DS_Store
__pycache__/
*.pyc

# Node / TypeScript build (Symphony orchestrator, U1+)
node_modules/
dist/
*.tsbuildinfo

# OpenSymphony engine per-run artifacts
.opensymphony/
.opensymphony.after_create.json
