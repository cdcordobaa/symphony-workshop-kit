{
  "name": "symphony-mvp",
  "version": "0.1.0",
  "description": "Symphony orchestrator MVP — walking skeleton (Notion + Claude Code)",
  "type": "module",
  "private": true,
  "bin": {
    "symphony": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node ./dist/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "liquidjs": "^10.21.0",
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
